version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"  # Adjust to match your project's Python version

sphinx:
  configuration: docs/conf.py  # Path to your conf.py file
  
python:
  install:
    - method: pip
      path: .
    - requirements: pandemic.egg-info/requires.txt
