version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"  # Adjust to match your project's Python version

python:
  install:
    - method: pip
      path: .
    - requirements: requirements.txt
